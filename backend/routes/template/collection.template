<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>{% block title %}Measur3D - OGC API Features{% endblock %}</title>
</head>

<body>

<h1>Metadata about the feature collection</h1>

<h2>Collection</h2>
<h3>{{ collection.title }} ({{ collection.title }})</h3>
<p>{{ collection.title }}</p>

<h4>Links for the collection</h4>
<p>CityObjects = <a href="{{ collection.url }}/collections/{{ collection.title }}/items">{{ collection.title }}</a> (application/json)</p>

<h2>Links</h2>

<p>
    self =
<a href="{{ collection.url }}/collections/{{ collection.title }}">this document</a>
(text/html)
</p>
<p>
    alternate =
<a href="{{ collection.url }}/collections/{{ collection.title }}?f=json">this document as JSON</a>
(application/json)
</p>


</body>
</html>
